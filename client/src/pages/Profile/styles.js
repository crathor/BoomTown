const styles = theme => ({
  content: {
    paddingTop: 0
  },
  header: {
    paddingBottom: 0
  },
  profileHeader: {
    padding: theme.spacing.unit * 6,
    marginBottom: theme.spacing.unit * 6
  }
})

export default styles
